import React from 'react';
import ReactDOM from 'react-dom/client';
import ResistorColorCoding from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <ResistorColorCoding />
  </React.StrictMode>
);
